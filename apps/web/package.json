{
  "name": "@fos/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Static landing site + SDK demo",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "echo 'static site — no typecheck needed'",
    "lint": "echo 'lint not yet configured'",
    "test": "echo 'no tests'"
  },
  "dependencies": {
    "@fos/sdk": "workspace:*"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
